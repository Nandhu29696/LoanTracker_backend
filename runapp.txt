#setup environment in local machine
pip install virtualenvwrapper-win

#create virtual env
mkvirtualenv loanenv

#active & deactive env
workon loanenv
deactivate

#collect nessery tools from pip
pip freeze > requirements.txt

#Install nessery tools
pip install -r requirements.txt

#run the django application
python manage.py runserver 0.0.0.0:8000

#create database tables
python manage.py makemigrations
python manage.py migrate

#create a new project
python manage.py startapp cricket

#create a super user
python manage.py createsuperuser

#deplyment to ec2 file
cd branch-repo
git pull origin branch-name
source venv/bin/activate

pm2 start python3 --name "django-server" -- manage.py runserver 0.0.0.0:8000
